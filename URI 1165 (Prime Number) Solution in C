 

#include<stdio.h>
int main()
{
    int t,n,k,count;
    scanf("%d",&t);
    while(t--)
    {
        if(t==-1) break;
        count=0;
 
        scanf("%d",&n);
        for(k=2; k<n; k++)
        {
            if(n%k==0) count++;
        }
        if(count==0) printf("%d eh primo\n",n);
        else printf("%d nao eh primo\n",n);
    }
    return 0;
}
